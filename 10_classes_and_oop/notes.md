# javascript and classes

## OOP

## Object 
- collection of properties and methods
-toLowerCase

## why use OOP

## parts of OOP

Object literal - literally object

- Constructor function : new keyword is constructor function - it allows us to make multiple instances from one object literal. It gives us new instance.
new keyword - an empty object is created, constructor function is called due to new keyword, in this keyword, every parameter is injected.

* constructor property - is the reference of itself.
- Prototypes
- Classes
- Instances (new, this)

## 4 pillars

Abstranction - hidding details eg- fetch() - behind the scene fetch() does a lots of work
Encapsulation - making a wrapper on top of something eg, capsule - it has powerful medicines inside but from outside it's a wrapper.
Inheritance - 
Polymorphism - ek naam anek kaam.